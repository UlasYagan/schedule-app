export enum Severity {
    error = 'error',
    info = 'info',
    success = 'success',
    warning = 'warning'
}

export enum Variant {
    filled = 'filled',
    outlined = 'outlined',
    standard = 'standard'
}

export enum Color {
    error = 'error',
    info = 'info',
    success = 'success',
    warning = 'warning'
}

export enum KeyEnum {
    users = 'users',
    employees = 'employees',
}